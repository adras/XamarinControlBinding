<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:XamarinControlBinding.ViewModels"
             xmlns:ctrl="clr-namespace:XamarinControlBinding.Controls"
             x:Class="XamarinControlBinding.MainPage">
    <ContentPage.BindingContext>
        <vm:MainPageModel />
    </ContentPage.BindingContext>
    <StackLayout>
        <!-- Important it needs to be a TwoWay Binding, otherwise when the model is changed, the Entry fields are not updated -->
        <ctrl:LabeledDecimalBox LabelText="Expected Value:" EntryValue="{Binding Soll, Mode=TwoWay}"></ctrl:LabeledDecimalBox>
        <ctrl:LabeledDecimalBox LabelText="Actual Value:" EntryValue="{Binding Ist, Mode=TwoWay}"></ctrl:LabeledDecimalBox>
        <ctrl:LabeledDecimalBox LabelText="Difference:" EntryValue="{Binding Difference}"></ctrl:LabeledDecimalBox>
        <StackLayout Orientation="Horizontal">
            <Button Text="Add Random values" Command="{Binding NextCommand}"></Button>
        </StackLayout>
    </StackLayout>

</ContentPage>
